import { StatusDirective } from './status.directive';

describe('StatusOrderDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusDirective();
    expect(directive).toBeTruthy();
  });
});
